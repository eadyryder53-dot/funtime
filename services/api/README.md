# API Server

Express API server (TypeScript). Responsible for:
- Auth/signup endpoints
- Messaging APIs
- Key management endpoints
- Admin tooling endpoints
- Media session coordination

Modules live under `src/modules`.
